<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="board">
    <select id="selectNotice" resultType="map" parameterType="string">
        <![CDATA[
            SELECT rownum, post.*
            FROM (
                     SELECT cp.CSPOSTNUM, cp.title, TO_CHAR(cp.CREATEDAT, 'yyyy-mm-dd')
                     FROM CSPOST cp
                     WHERE csPostType = #{csPostType}
                     ORDER BY CREATEDAT DESC
                 ) post;
        ]]>
    </select>
</mapper>