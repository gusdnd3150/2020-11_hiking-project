<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="userMapper">
	<!-- <resultMap id="userInsertResult01" type="userVO">
		<result property="userNum" column="userNum" />
		<result property="id" column="id" />
		<result property="password" column="password" />
		<result property="name" column="name" />
		<result property="sex" column="sex" />
		<result property="phone" column="phone" />
		<result property="address" column="address" /> 
		<result property="email" column="email" />
		<result property="type" column="type" /> 
		<result property="status" column="status" />
		<result property="createdAt" column="createdAt" />
		<result property="updatedAt" column="updatedAt" /> 
	</resultMap> -->
	
	<resultMap id="logInResult01" type="userVO">
	<result property="id" column="userId"/>
	<result property="password" column="userPwd"/>
	</resultMap>
	
	
	
      <insert id="insertUser" parameterType="userVO">
      <![CDATA[
      INSERT INTO USERS(userNum, id , password, name, sex, phone, address, email, type, status, createdAt)
      VALUES (users_SEQ.nextVal, #{id}, #{password}, #{name}, #{sex}, #{phone}, #{address}, #{email}, 200, 10, sysdate)
      ]]>
	   </insert> 
	   
	   <select id="idCheck" parameterType="String" resultType="int">
	   <![CDATA[
	    select count(*) 
	    from users 
	    where id = #{id} and status=10
	    ]]>
	   </select>
	   
	   <select id="logIn" resultMap="logInResult01">
	    <![CDATA[
	    select id, password
	    from users where id=#{id} And password=#{password} and status=10
	    
	    ]]>
	    </select>
	   
	   <update id="updateUser" parameterType="String">
	  <![CDATA[
	   update users 
	   set name=#{name}, password=#{password} , sex=#{sex}, phone=#{phone}, address=#{address}, email=#{email}, updatedAt=systimestamp
	   where id=#{id}
	        ]]>
	   </update>
   
	   
	   
</mapper>